/**
 * DataPoint interface
 * @export DataPoint interface
 * @interface DataPoint
 * @property {number} x - x coordinate
 * @property {number} y - y coordinate
 */
export interface DataPoint {
    x: number;
    y: number;
  }